// .forEach

const array = [1,2,3,4,5,6]

array.forEach((item) => {
  console.log(item);
})

// FOR LOOP

for (let i=0; i < array.length; i++) {
  console.log(array[i]);
}

// THESE ARE THE SAME but .foreach is an array method
